import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'hackathon_covadonga_patient',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
